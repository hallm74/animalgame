import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="bg-blue-100 p-4 rounded-lg shadow-md">
            <h1 className="text-2xl font-bold text-center">Guess the Animal!</h1>
        </header>
    );
};

export default Header;